% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pivotkz.R
\name{pivotkz}
\alias{pivotkz}
\title{Convert a dataset from wide to long format}
\usage{
pivotkz(data, cols, value_name, names_to = "year", prefix = "X")
}
\arguments{
\item{data}{A data frame or tibble containing the data to reshape.}

\item{cols}{Columns to pivot into long format (e.g., `starts_with("X")` or `2:10`).}

\item{value_name}{A string giving the name of the new value column in the long dataset.}

\item{names_to}{A string. The name for the column that stores the original column names. Default is `"year"`.}

\item{prefix}{A string prefix to strip from column names before converting them to numeric. Default is `"X"`.}
}
\value{
A tibble in long format, with identifier columns preserved, and the
  selected `cols` pivoted into two columns: one named by `names_to`, and
  one named by `value_name`.

A tibble in long format, with identifier columns preserved, and the
  selected `cols` pivoted into two columns: one named by `names_to`, and
  one named by `value_name`.
}
\description{
Convert a dataset from wide to long format
}
\examples{
Convert a dataset from wide to long format

library(dplyr)
library(tidyr)

# Example 1: Simple dataset
df <- tibble(
  country = c("A", "B"),
  X2000 = c(1, 2),
  X2001 = c(3, 4)
)
pivotkz(df, starts_with("X"), "value")

Example 2: Using the maternal_mortality dataset included in the package
data(maternal_mortality)
head(maternal_mortality)
long_mm <- pivotkz(
  data = maternal_mortality,
  cols = starts_with("X"),
  value_name = "mortality_ratio"
)
head(long_mm)

}
